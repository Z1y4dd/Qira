---
phase: 03-placement-vertical
plan: 01
slice_name: wave-0-foundation
type: execute
wave: 0
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/migrations/0001_phase3_placement.sql
  - src/components/ui/alert-dialog.tsx
  - src/components/placement/escape-hatch.tsx
  - src/services/placement.test.ts
  - src/services/placement.integration.test.ts
  - src/db/seed/placement-placeholder.test.ts
  - tests/invariants/placement-bundle-leak.test.ts
  - tests/e2e/placement-cross-parent.spec.ts
  - tests/e2e/placement-escape-hatch.spec.ts
  - tests/e2e/placement-flow.spec.ts
  - scripts/db-verify-columns.ts
  - package.json
autonomous: true
requirements:
  - PLAC-01
  - PLAC-02
  - PLAC-03
  - PLAC-04
  - PLAC-05
  - PLAC-06
  - PLAC-07
  - PLAC-08
threat_refs:
  - T-3-schema-drift
  - T-3-bundle-leak
  - T-3-rls-cross-parent
mode: mvp

must_haves:
  truths:
    - "All 8 Wave 0 test files exist on disk and are runnable (they may fail — they are stubs)."
    - "shadcn AlertDialog component exists at src/components/ui/alert-dialog.tsx."
    - "EscapeHatch component shell exists at src/components/placement/escape-hatch.tsx with the mode prop API and AlertDialog confirmation flow (action wiring stubbed — Plan 05 wires abortPlacementAction)."
    - "src/db/schema.ts compiles with the 4 new attempts columns, 1 new attempt_answers column, and is_placeholder on texts + questions."
    - "drizzle/migrations/0001_phase3_placement.sql exists and contains only ADD COLUMN / CREATE TYPE statements (no DROP, no type changes)."
    - "Live Supabase DB has the new columns: scripts/db-verify-columns.ts exits 0."
    - "pnpm typecheck and pnpm lint pass after the schema additions."
  artifacts:
    - path: src/components/ui/alert-dialog.tsx
      provides: "shadcn AlertDialog primitives (AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogAction, AlertDialogCancel, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader)"
      contains: "AlertDialog"
    - path: src/components/placement/escape-hatch.tsx
      provides: "Client Component shell — two floating buttons + AlertDialog 2-tap confirmation. Action wiring is a stub no-op with a PLAN-05 marker; Plan 05 wires abortPlacementAction. Component can be embedded by Plan 04 on result page in wave 3."
      contains: "'use client'"
    - path: drizzle/migrations/0001_phase3_placement.sql
      provides: "Generated migration: ALTER TABLE attempts ADD COLUMN escape_hatched, escape_hatched_at, escape_hatched_reason, placement_bank_version; ALTER TABLE attempt_answers ADD COLUMN choice_order; ALTER TABLE texts ADD COLUMN is_placeholder; ALTER TABLE questions ADD COLUMN is_placeholder; CREATE TYPE escape_hatch_reason."
    - path: scripts/db-verify-columns.ts
      provides: "Verification script that queries information_schema.columns for the 6 new columns and exits non-zero if any are missing."
    - path: src/services/placement.test.ts
      provides: "Vitest stub: imports from @/services/placement, RED tests for assignLevel + deterministicShuffle + abortPlacement fallback formula."
    - path: src/services/placement.integration.test.ts
      provides: "Vitest stub: integration tests for getPlacementState, startPlacement, recordPlacementAnswer, abortPlacement, resetPlacement, seed count assertions."
    - path: src/db/seed/placement-placeholder.test.ts
      provides: "Vitest stub: asserts texts.isPlaceholder=true count = 5, questions.isPlaceholder=true count = 15."
    - path: tests/invariants/placement-bundle-leak.test.ts
      provides: "Vitest invariant: greps src/app/**/*.{ts,tsx} and src/components/**/*.{ts,tsx} for /\\bisCorrect\\b/, fails if found."
    - path: tests/e2e/placement-cross-parent.spec.ts
      provides: "Playwright stub: Parent A completes placement, Parent B asserts zero visible attempts rows + cookie forge → /choose-child."
    - path: tests/e2e/placement-escape-hatch.spec.ts
      provides: "Playwright stub: navigates through placement, asserts escape-hatch buttons visible on passage + question + (after-completion) reader screens."
    - path: tests/e2e/placement-flow.spec.ts
      provides: "Playwright stub: happy-path full-loop placement E2E covering PLAC-01 (gate redirect), PLAC-05 (result screen + parent-manage assignment display). Plan 06 fills bodies."
  key_links:
    - from: src/db/schema.ts
      to: drizzle/migrations/0001_phase3_placement.sql
      via: pnpm db:generate
      pattern: "drizzle-kit generate diffs schema.ts against DIRECT_DATABASE_URL and emits SQL"
    - from: drizzle/migrations/0001_phase3_placement.sql
      to: Supabase Postgres (cloud)
      via: pnpm db:migrate
      pattern: "drizzle-kit migrate applies pending SQL files via DIRECT_DATABASE_URL"
    - from: scripts/db-verify-columns.ts
      to: information_schema.columns
      via: postgres.js SQL query
      pattern: "SELECT column_name FROM information_schema.columns WHERE table_name IN ('attempts','texts','questions','attempt_answers')"
---

<objective>
Wave 0 — shared infrastructure for Phase 3. This is the only horizontal slice. It lands the additive schema migration (4 columns on `attempts`, 1 column on `attempt_answers`, 1 column on `texts`, 1 column on `questions`, 1 new enum) against the live Supabase DB; installs the shadcn `alert-dialog` primitive that the escape-hatch component imports; **scaffolds the EscapeHatch Client Component shell** (Plan 04's result page embeds it in wave 3; Plan 05's wave-4 actions wiring fills the abort-action body); stubs all 8 RESEARCH-prescribed test files so every subsequent wave plan can validate against existing files; and adds a `db:verify-columns` script + `pnpm` script so the schema-drift threat (T-3-schema-drift) is observable.

Purpose: Eliminates the silent-failure mode where TypeScript compiles against schema.ts but the live DB lacks the columns. Eliminates the wave-ordering trap where Plan 04's result page needs `<EscapeHatch>` (SC4 — visible during AND after placement) but the component used to be created in wave-4 Plan 05. Every Wave 1+ plan can now assume: schema is live, AlertDialog exists, EscapeHatch shell is importable, test scaffolds are runnable.

Output: Live Supabase DB has the 6 new columns + 1 new enum; the shadcn AlertDialog component is importable; the EscapeHatch component shell is importable and renders the two floating buttons + the AlertDialog with placeholder action wiring (Plan 05 replaces the placeholder); 8 test files exist as stubs; `pnpm db:verify-columns` exits 0.
</objective>

<execution_context>
@/home/ziyad/kids-AZ/.claude/get-shit-done/workflows/execute-plan.md
@/home/ziyad/kids-AZ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-placement-vertical/03-CONTEXT.md
@.planning/phases/03-placement-vertical/03-RESEARCH.md
@.planning/phases/03-placement-vertical/03-PATTERNS.md
@src/db/schema.ts
@src/db/seed/index.ts
@scripts/db-verify.ts
@tests/invariants/auth-getsession-ban.test.ts
@tests/e2e/auth-cross-user.spec.ts
@tests/e2e/_helpers/test-parents.ts

<reconciliation>
RESEARCH found a discrepancy with CONTEXT D-07: the `attempts` table ALREADY has `assignedLevelId: uuid('assigned_level_id').references(() => levels.id)` (line 298 of schema.ts). Do NOT add a redundant `assigned_level` integer column. Use the existing `assignedLevelId` FK; the service layer resolves `levels.number` via join. Only the OTHER columns from D-07 are new — `escape_hatched`, `escape_hatched_at`, `escape_hatched_reason`, `placement_bank_version`, plus `is_placeholder` on `texts` + `questions`, and `choice_order` on `attempt_answers` (per RESEARCH §B).

**Wave-ordering reconciliation (revision 1, addressing BLOCKER 3 / SC4 "after placement"):** the EscapeHatch Client Component originally lived in Plan 05 (wave 4). Phase 3 SC4 + PLAC-06 require the hatch to be visible on the result page (the ONLY post-assessment Phase 3 surface), which is created by Plan 04 in wave 3. To unblock that without restructuring waves, the component **shell** (file, 'use client' directive, mode prop API, two floating buttons, AlertDialog confirmation modal, stubbed action handler) lands here in Wave 0. AlertDialog is the only runtime dep and is also installed here. Plan 05 replaces the stub action handler with the real `abortPlacementAction.bind(null, ...)` call once that action exists (Plan 05 wave 4). Plan 04 embeds the component on PassageScreen, QuestionScreen, AND the result page in wave 3 — every screen renders the hatch with the stub handler active until Plan 05 lands. The stub handler closes the dialog and logs a `console.warn('escape-hatch action wiring pending Plan 05')` — kid-visible UX is identical; only the server-side abort is deferred.
</reconciliation>

<interfaces>
<!-- Types and enums the schema migration must add. Drizzle types are inferred — these are the column declarations executors will paste into the existing table builders in src/db/schema.ts. -->

New pgEnum (add near line 33 after the existing two enums):
- `escapeHatchReason = pgEnum('escape_hatch_reason', ['too_hard', 'too_easy'])`

New imports to add to the import block (lines 17–27 of schema.ts):
- `boolean, jsonb` from `drizzle-orm/pg-core` — these two are not yet imported.

Columns to add to the `attempts` table builder (alongside existing `assignedLevelId`, `score`, `finishedAt`):
- `escapeHatched: boolean('escape_hatched').default(false).notNull()`
- `escapeHatchedAt: timestamp('escape_hatched_at', { withTimezone: true })`
- `escapeHatchedReason: escapeHatchReason('escape_hatched_reason')`
- `placementBankVersion: integer('placement_bank_version')`

Columns to add to the `attempt_answers` table builder:
- `choiceOrder: jsonb('choice_order')`

Columns to add to the `texts` table builder:
- `isPlaceholder: boolean('is_placeholder').default(false).notNull()`

Columns to add to the `questions` table builder:
- `isPlaceholder: boolean('is_placeholder').default(false).notNull()`

No new RLS policies (no new tables). The existing RLS policies on these tables cover the new columns automatically.

EscapeHatch component shell (Task 1.7) — Plan 05 fills in the action call:

```typescript
'use client';
export type EscapeHatchMode = 'placement' | 'reader';
export interface EscapeHatchProps {
  mode: EscapeHatchMode;
  attemptId?: string;
  childId?: string;
}
export function EscapeHatch(props: EscapeHatchProps): JSX.Element;
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Install shadcn AlertDialog primitive</name>
  <files>src/components/ui/alert-dialog.tsx, package.json</files>
  <read_first>
    - src/components/ui/dialog.tsx (analog — confirms project shadcn conventions: same Radix primitive style, `end-4 top-4` logical-property positioning, `cn()` helper)
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §"No Analog Found" (records that AlertDialog is absent and must be installed in Wave 0)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §E (escape-hatch uses AlertDialog with `mode` prop)
  </read_first>
  <action>Run `pnpm dlx shadcn@latest add alert-dialog` (project was init'd with `--rtl` in Phase 1, so the component arrives with RTL conventions baked in). This installs `src/components/ui/alert-dialog.tsx` re-exporting `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogCancel`, `AlertDialogAction`. If the CLI adds `@radix-ui/react-alert-dialog` to `devDependencies`, move it to `dependencies` (runtime import). Do NOT edit any existing component or page.</action>
  <verify>
    <automated>test -f src/components/ui/alert-dialog.tsx &amp;&amp; grep -E '^export.*AlertDialog' src/components/ui/alert-dialog.tsx | grep -v '^#' &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/alert-dialog.tsx` exists.
    - File exports identifiers: `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogCancel`, `AlertDialogAction` (grep returns ≥ 9 export lines).
    - `@radix-ui/react-alert-dialog` appears in `package.json` `dependencies` (not `devDependencies`).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>shadcn AlertDialog is importable from `@/components/ui/alert-dialog` and the project still typechecks and lints clean.</done>
</task>

<task type="auto">
  <name>Task 1.2: Add Phase 3 columns + enum to src/db/schema.ts</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts (full file — confirm existing `attempts.assignedLevelId` line 298, existing pgEnum lines 32–33, the import block lines 17–27, and the `texts`/`questions` `withCheck: sql\`false\`` RLS policies)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §B "Schema additions for Phase 3" (canonical column list)
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`src/db/schema.ts` (the additive column pattern and the assignedLevelId reconciliation note)
  </read_first>
  <action>Modify `src/db/schema.ts` in place. (1) Add `boolean, jsonb` to the existing `drizzle-orm/pg-core` import on lines 17–27. (2) After the existing two `pgEnum` declarations (around line 33), add `export const escapeHatchReason = pgEnum('escape_hatch_reason', ['too_hard', 'too_easy']);`. (3) Inside the `attempts` table column builder (after `finishedAt` on line 301), add the four new columns from `<interfaces>` block exactly: `escapeHatched`, `escapeHatchedAt`, `escapeHatchedReason`, `placementBankVersion`. (4) Inside `attempt_answers` column builder (after `answeredAt`), add `choiceOrder: jsonb('choice_order')`. (5) Inside `texts` column builder (after `createdAt` on line 172), add `isPlaceholder: boolean('is_placeholder').default(false).notNull()`. (6) Inside `questions` column builder (after `position` on line 214), add `isPlaceholder: boolean('is_placeholder').default(false).notNull()`. Do NOT add any new pgPolicy entries — the existing table-level RLS covers the new columns. Do NOT add a redundant `assigned_level` integer column on `attempts` — `assignedLevelId` is already present (per D-07 reconciliation).</action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; grep -q "escape_hatch_reason" src/db/schema.ts &amp;&amp; grep -q "escapeHatched" src/db/schema.ts &amp;&amp; grep -q "isPlaceholder" src/db/schema.ts &amp;&amp; grep -q "choiceOrder" src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm lint` exits 0.
    - schema.ts contains identifiers: `escapeHatched`, `escapeHatchedAt`, `escapeHatchedReason`, `placementBankVersion`, `choiceOrder`, `isPlaceholder` (each grep returns ≥ 1 match).
    - schema.ts contains `pgEnum('escape_hatch_reason', ['too_hard', 'too_easy'])`.
    - schema.ts does NOT contain `assigned_level` snake-case (would indicate a redundant integer column was added).
    - `tests/invariants/rls-coverage.test.ts` passes (no new tables, RLS coverage unchanged).
  </acceptance_criteria>
  <done>Schema.ts has the 6 new columns + new enum. TypeScript compiles. The existing RLS invariant still passes.</done>
</task>

<task type="auto">
  <name>Task 1.3: Generate Phase 3 migration SQL with drizzle-kit</name>
  <files>drizzle/migrations/0001_phase3_placement.sql, drizzle/migrations/meta/_journal.json, drizzle/migrations/meta/0001_snapshot.json</files>
  <read_first>
    - drizzle/migrations/0000_workable_trauma.sql (Phase 1 baseline — confirm SQL style, that ALTER TABLE statements are the expected form, no quoted identifiers, etc.)
    - drizzle.config.ts (confirm output dir = `drizzle/migrations`, driver = postgres.js, uses DIRECT_DATABASE_URL)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §B "Migration vs. push" (review-before-migrate gate)
  </read_first>
  <action>Run `pnpm db:generate` (alias for `drizzle-kit generate`). Drizzle inspects the live DB via `DIRECT_DATABASE_URL`, diffs against schema.ts, and emits a new file `drizzle/migrations/000N_<random_slug>.sql`. Rename it to `0001_phase3_placement.sql` (preserving the numeric ordinal `0001` for drizzle-kit's journal). Update `drizzle/migrations/meta/_journal.json` if the rename requires (drizzle-kit re-writes journal automatically when `generate` runs — confirm the journal entry's `tag` field matches the new filename without the `.sql`). Open the SQL file and confirm it contains ONLY: (a) `CREATE TYPE "public"."escape_hatch_reason" AS ENUM('too_hard', 'too_easy');`, (b) `ALTER TABLE "attempts" ADD COLUMN "escape_hatched" boolean DEFAULT false NOT NULL;`, (c) `ALTER TABLE "attempts" ADD COLUMN "escape_hatched_at" timestamp with time zone;`, (d) `ALTER TABLE "attempts" ADD COLUMN "escape_hatched_reason" "public"."escape_hatch_reason";`, (e) `ALTER TABLE "attempts" ADD COLUMN "placement_bank_version" integer;`, (f) `ALTER TABLE "attempt_answers" ADD COLUMN "choice_order" jsonb;`, (g) `ALTER TABLE "texts" ADD COLUMN "is_placeholder" boolean DEFAULT false NOT NULL;`, (h) `ALTER TABLE "questions" ADD COLUMN "is_placeholder" boolean DEFAULT false NOT NULL;`. If any `DROP`, `ALTER COLUMN`, or unexpected statement appears, STOP and investigate — schema.ts has drift from the live DB and was not addressed. Do NOT apply the migration in this task; that's Task 1.4 (the BLOCKING step).</action>
  <verify>
    <automated>test -f drizzle/migrations/0001_phase3_placement.sql &amp;&amp; ! grep -E -i 'drop|alter column' drizzle/migrations/0001_phase3_placement.sql &amp;&amp; grep -c 'ADD COLUMN' drizzle/migrations/0001_phase3_placement.sql | grep -qE '^[67]$' &amp;&amp; grep -q 'CREATE TYPE' drizzle/migrations/0001_phase3_placement.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `drizzle/migrations/0001_phase3_placement.sql` exists.
    - File contains exactly one `CREATE TYPE` statement for `escape_hatch_reason`.
    - File contains 6 or 7 `ADD COLUMN` statements (6 if Drizzle merges the two `is_placeholder` ALTERs into one statement, 7 if it splits them — both shapes are acceptable).
    - File contains ZERO `DROP` statements (case-insensitive grep returns no matches).
    - File contains ZERO `ALTER COLUMN` statements (case-insensitive grep returns no matches).
    - `drizzle/migrations/meta/_journal.json` lists the new tag.
  </acceptance_criteria>
  <done>Reviewable additive-only SQL file is committed to the repo, ready to be applied via `pnpm db:migrate` in Task 1.4.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Task 1.4: Apply migration to live Supabase + verify columns landed</name>
  <files>scripts/db-verify-columns.ts, package.json</files>
  <read_first>
    - scripts/db-verify.ts (exact analog — same dotenv + postgres.js shape)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §"Post-migration verification query" (the canonical information_schema.columns query)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §"Anti-pitfall 1" (the false-positive migration verification failure mode this gate prevents)
    - drizzle/migrations/0001_phase3_placement.sql (the SQL about to be applied)
  </read_first>
  <action>This is the [BLOCKING] threat-mitigation task for T-3-schema-drift. (Step A) Create `scripts/db-verify-columns.ts` following `scripts/db-verify.ts` exactly: import `config` from `dotenv`, `postgres` from `postgres`; throw if `DIRECT_DATABASE_URL` is missing; query `information_schema.columns` for the 4 new columns on `attempts` (`escape_hatched`, `escape_hatched_at`, `escape_hatched_reason`, `placement_bank_version`), the 1 new column on `attempt_answers` (`choice_order`), and `is_placeholder` on `texts` and `questions`. Also query `pg_type` for `escape_hatch_reason`. If ANY expected column or type is absent, print which is missing and `process.exit(1)`. Otherwise print "Phase 3 schema verified" and exit 0. (Step B) Add `"db:verify-columns": "tsx scripts/db-verify-columns.ts"` to package.json `scripts` (alphabetically near the existing `db:*` scripts). (Step C) Run `pnpm db:migrate` to apply `0001_phase3_placement.sql` to the live Supabase DB via `DIRECT_DATABASE_URL`. (Step D) Run `pnpm db:verify-columns` and confirm exit 0. If exit non-zero, the migration did not apply — DO NOT proceed to any subsequent Phase 3 plan until this is resolved. This task is the gate the rest of Phase 3 depends on (T-3-schema-drift mitigation per security threat model).</action>
  <verify>
    <automated>test -f scripts/db-verify-columns.ts &amp;&amp; grep -q '"db:verify-columns"' package.json &amp;&amp; pnpm db:verify-columns</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/db-verify-columns.ts` exists and imports `postgres` + reads `DIRECT_DATABASE_URL` (matches db-verify.ts shape).
    - `pnpm db:verify-columns` exits 0.
    - The script's stdout names all 7 new columns + the `escape_hatch_reason` enum (grep test on captured output).
    - `package.json` `scripts` contains `db:verify-columns`.
    - Re-running `pnpm db:migrate` is a no-op (idempotent — drizzle-kit's journal records the applied migration).
    - **T-3-schema-drift mitigation:** live DB now matches schema.ts. Subsequent Wave 1 plans can write to the new columns without runtime errors.
  </acceptance_criteria>
  <done>Live Supabase DB has the 6 new columns + new enum. `pnpm db:verify-columns` is the standing gate for any future migration. Wave 1 unblocked.</done>
</task>

<task type="auto">
  <name>Task 1.5: Stub the 4 Vitest test files for Wave 1 to extend</name>
  <files>src/services/placement.test.ts, src/services/placement.integration.test.ts, src/db/seed/placement-placeholder.test.ts, tests/invariants/placement-bundle-leak.test.ts</files>
  <read_first>
    - tests/invariants/auth-getsession-ban.test.ts (exact analog for the bundle-leak invariant — same tinyglobby + regex + per-file describe structure)
    - tests/unit/profiles-service.test.ts (if exists — else use the existing service test pattern visible in src/services/profiles.ts) — analog for the pure-function unit shape
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §F "Vitest unit tests" and §F "Vitest invariant tests" (canonical test surface)
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`src/services/placement.test.ts` and §`tests/invariants/placement-bundle-leak.test.ts` (analog mappings)
  </read_first>
  <action>Create 4 Vitest stub files. Each file must be IMMEDIATELY RUNNABLE under `pnpm test:run` but the test bodies use `test.todo()` or `expect(true).toBe(false)` with a TODO comment so they show as failing/pending until the Wave 1 plan that owns them fills them in (Plans 03 and 04). (1) `src/services/placement.test.ts` — pure-fn unit test stub: `import { describe, test } from 'vitest';` + describe blocks `assignLevel()` with 6 `test.todo` cases from RESEARCH §A coverage matrix (all-correct G1-2, all-wrong G1-2, mixed L2+L6 G1-2, exactly-60% L10 G3-4, pre-K all-wrong, edge: empty passageResults). Plus a separate describe for `deterministicShuffle()` with 3 `test.todo` cases (idempotent, different seeds → different orders, all items preserved). Plus a describe for `abortPlacement` formula with 3 `test.todo` cases (too_hard, too_easy, too_easy-at-window-max). (2) `src/services/placement.integration.test.ts` — integration stub: `import { describe, test } from 'vitest';` + 5 `test.todo` cases for `getPlacementState` (not_started, in_progress, completed, escape_hatched, reset). (3) `src/db/seed/placement-placeholder.test.ts` — seed correctness stub: 2 `test.todo` cases (5 placeholder texts, 15 placeholder questions). (4) `tests/invariants/placement-bundle-leak.test.ts` — IMPLEMENT FULLY (this is an invariant gate that must pass from day one, before any placement UI exists, so it can never regress): copy the structure of `tests/invariants/auth-getsession-ban.test.ts`, set `PATTERN = /\bisCorrect\b/`, set `globSync` target to `['src/app/**/*.{ts,tsx}', 'src/components/**/*.{ts,tsx}']`, `ALLOW_LIST = new Set([])`. The describe label is `'placement bundle leak'`. The grep gate ensures `isCorrect` never appears in client-visible code (T-3-bundle-leak mitigation).</action>
  <verify>
    <automated>test -f src/services/placement.test.ts &amp;&amp; test -f src/services/placement.integration.test.ts &amp;&amp; test -f src/db/seed/placement-placeholder.test.ts &amp;&amp; test -f tests/invariants/placement-bundle-leak.test.ts &amp;&amp; pnpm test:run tests/invariants/placement-bundle-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist on disk.
    - `pnpm test:run tests/invariants/placement-bundle-leak.test.ts` exits 0 (the invariant is fully implemented and passes — no `isCorrect` in src/app or src/components yet, so it trivially passes).
    - The 3 stub Vitest files import from `vitest` and use `test.todo` (grep for `test.todo` returns ≥ 11 matches across the 3 files).
    - `pnpm test:run` does not error on the stub files (todo tests are reported as pending, not failures).
    - **T-3-bundle-leak mitigation:** the grep gate is live from Wave 0 forward.
  </acceptance_criteria>
  <done>Wave 1 plans (03, 04) can fill in the test bodies as their first sub-task. The bundle-leak invariant is enforced from this point on.</done>
</task>

<task type="auto">
  <name>Task 1.6: Stub the 3 Playwright E2E spec files for Wave 1+ to extend</name>
  <files>tests/e2e/placement-cross-parent.spec.ts, tests/e2e/placement-escape-hatch.spec.ts, tests/e2e/placement-flow.spec.ts</files>
  <read_first>
    - tests/e2e/auth-cross-user.spec.ts (exact analog — cross-parent isolation pattern)
    - tests/e2e/_helpers/test-parents.ts (the createTestParent/deleteTestParent helpers Phase 3 reuses)
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`tests/e2e/placement-cross-parent.spec.ts` and §`tests/e2e/placement-escape-hatch.spec.ts`
    - .planning/phases/03-placement-vertical/03-VALIDATION.md §"Per-Task Verification Map" (PLAC-01 + PLAC-05 mapped to `pnpm e2e --grep 'placement-flow'`)
  </read_first>
  <action>Create 3 Playwright stub specs. Each file is runnable under `pnpm e2e --grep <name>` but test bodies use `test.fixme()` (Playwright's "expected to fail until implemented" marker — does not fail the suite, only reports as fixme) until the downstream plans fill them in. (1) `tests/e2e/placement-cross-parent.spec.ts` — `import { test } from '@playwright/test';` + describe `placement cross-parent isolation` with 2 `test.fixme` cases: ("Parent A completes placement, Parent B cannot read A's attempts row", "Parent B forges qira_active_child cookie to A's child UUID → redirected to /choose-child"). Include `test.describe.serial()` wrapper. (2) `tests/e2e/placement-escape-hatch.spec.ts` — `import { test } from '@playwright/test';` + describe `placement escape hatch visibility` with 5 `test.fixme` cases: ("hatch visible on /placement/start", "hatch visible on passage screen", "hatch visible on question screen", "hatch visible on /placement/{attemptId}/result after placement completes — SC4 'after placement'", "hatch visible on (active)/dashboard for already-placed child — Phase 4 reuse"). Use `aria-label`-based assertions in the test body sketches as comments inside each `test.fixme` (e.g., `// expect(page.getByRole('button', { name: /هذا صعب/ })).toBeVisible()`). (3) `tests/e2e/placement-flow.spec.ts` — NEW spec covering PLAC-01 (gate redirect) and PLAC-05 (full-loop result + parent-manage assignment display). `import { test } from '@playwright/test';` + describe `placement flow — happy path` with 3 `test.fixme` cases: ("Unplaced child navigates to /dashboard → redirected to /placement/start" — PLAC-01 gate; "Full 15-question loop completes → result screen renders 'اخترنا لك المستوى' with a level number inside <bdi dir='ltr'>" — PLAC-05; "Parent on /profiles/[childId]/manage sees 'المستوى المُعيَّن: ٤' (or similar Western-digit level) after child completes placement" — PLAC-05 parent visibility). Each `test.fixme` body contains comments sketching the assertion shape (URLs to visit, role+name selectors, expected text fragments) so Plan 06 Task 6.3 (which fills these bodies) has unambiguous targets.</action>
  <verify>
    <automated>test -f tests/e2e/placement-cross-parent.spec.ts &amp;&amp; test -f tests/e2e/placement-escape-hatch.spec.ts &amp;&amp; test -f tests/e2e/placement-flow.spec.ts &amp;&amp; pnpm e2e --grep "placement" --list 2&gt;&amp;1 | grep -q "placement"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist on disk.
    - `pnpm e2e --grep placement --list` reports all three spec files and their test cases (count ≥ 10 cases total — 2 + 5 + 3).
    - `pnpm e2e --grep "placement-flow" --list` reports the 3 flow cases (the grep VALIDATION.md uses).
    - Each file uses `test.fixme` (grep returns ≥ 2 in cross-parent, ≥ 5 in escape-hatch, ≥ 3 in flow).
    - `pnpm e2e --grep placement` is callable without configuration error (fixme tests do not fail the run).
  </acceptance_criteria>
  <done>Wave 1+ plans 04, 05, 06 can replace `test.fixme` calls with real implementations. The VALIDATION.md grep target `placement-flow` is now a discoverable spec file. Playwright spec scaffolding exists from Wave 0.</done>
</task>

<task type="auto">
  <name>Task 1.7: Scaffold EscapeHatch component shell (action wiring stubbed for Plan 05)</name>
  <files>src/components/placement/escape-hatch.tsx</files>
  <read_first>
    - src/components/ui/alert-dialog.tsx (from Task 1.1 — confirms shadcn AlertDialog API)
    - src/app/(authenticated)/(picker)/profiles/[childId]/manage/delete-dialog.tsx (closest analog — Client component + AlertDialog trigger + useTransition; same shape minus the bound Server Action)
    - src/components/ui/button.tsx + src/components/arabic-text.tsx
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §E "Persistent Floating Escape-Hatch Component"
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`src/components/placement/escape-hatch.tsx`
  </read_first>
  <action>Create `src/components/placement/escape-hatch.tsx` with `'use client'` at the top. Imports: `useState, useTransition` from React; the AlertDialog primitives from `@/components/ui/alert-dialog`; `Button` from `@/components/ui/button`; `ArabicText` from `@/components/arabic-text`. Do NOT yet import `abortPlacementAction` — that action does not exist in Wave 0 (Plan 05 wave 4 creates it). Export `type EscapeHatchMode = 'placement' | 'reader'` and `interface EscapeHatchProps { mode: EscapeHatchMode; attemptId?: string; childId?: string }`. Component body uses `const [openReason, setOpenReason] = useState<'too_hard' | 'too_easy' | null>(null)` and `const [isPending, startTransition] = useTransition()`. Define a stubbed `handleConfirm` that closes the dialog and logs a console.warn: `startTransition(() => { console.warn('[PLAN-05] escape-hatch action wiring pending — confirmed reason:', openReason, 'mode:', props.mode); setOpenReason(null); });`. Add a literal source-code marker comment IMMEDIATELY ABOVE the handleConfirm body: `// PLAN-05-ACTION-WIRING: replace this stub with abortPlacementAction({ attemptId, reason }) for mode === 'placement', and shiftLevelAction({ childId, direction }) for mode === 'reader' (Phase 4).`. Markup (logical RTL utilities only): a fixed-position container `<div className="fixed end-4 bottom-4 z-50 flex flex-col gap-2">` with two `<Button variant="outline" size="sm" onClick={() => setOpenReason('too_hard' | 'too_easy')} aria-label="هذا صعب" or "هذا سهل">` siblings (one for too_hard with emoji 😟, one for too_easy with emoji 😊). Sibling `<AlertDialog open={openReason !== null} onOpenChange={(open) => { if (!open) setOpenReason(null); }}>` containing `<AlertDialogContent>` with `<AlertDialogHeader>` (Title: "هل أنت متأكد؟", Description: branch on openReason — "سنختار لك مستوى أسهل." for too_hard or "سنختار لك مستوى أصعب." for too_easy) and `<AlertDialogFooter>` with `<AlertDialogCancel disabled={isPending}>إلغاء</AlertDialogCancel>` and `<AlertDialogAction onClick={handleConfirm} disabled={isPending}>{isPending ? 'جارٍ…' : 'تأكيد'}</AlertDialogAction>`. All Arabic strings inside `<ArabicText size="ui">` (or `size="reader"` for the dialog title). The component is fully renderable in Wave 0 — the kid-visible UX (two buttons, modal open/close, Cancel returns control) is complete; only the server-side abort write is deferred to Plan 05. Plan 04 imports this component on PassageScreen + QuestionScreen + result page in wave 3.</action>
  <verify>
    <automated>test -f src/components/placement/escape-hatch.tsx &amp;&amp; grep -q "'use client'" src/components/placement/escape-hatch.tsx &amp;&amp; grep -q "EscapeHatch" src/components/placement/escape-hatch.tsx &amp;&amp; grep -q "PLAN-05-ACTION-WIRING" src/components/placement/escape-hatch.tsx &amp;&amp; grep -q "end-4 bottom-4" src/components/placement/escape-hatch.tsx &amp;&amp; ! grep -q "abortPlacementAction" src/components/placement/escape-hatch.tsx &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm build &amp;&amp; bash scripts/lint-rtl.sh &amp;&amp; pnpm test:run tests/invariants/placement-bundle-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/placement/escape-hatch.tsx` exists with `'use client'` directive at the top.
    - File exports `EscapeHatch`, `EscapeHatchMode`, `EscapeHatchProps`.
    - File does NOT import `abortPlacementAction` (that action doesn't exist yet in Wave 0 — grep returns 0).
    - File contains literal marker comment `PLAN-05-ACTION-WIRING` so Plan 05 has an unambiguous insertion point.
    - Container `<div>` uses `fixed end-4 bottom-4 z-50` (logical-property positioning per Tailwind v4 + RESEARCH §E).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm build` exits 0 (component is renderable in production build even without action wiring).
    - `bash scripts/lint-rtl.sh` exits 0.
    - `pnpm test:run tests/invariants/placement-bundle-leak.test.ts` exits 0 (component does not read `isCorrect`).
    - Manual smoke (optional): render `<EscapeHatch mode="placement" attemptId="test-id" />` in a sandbox page; tap a button → dialog opens; tap Cancel → dialog closes; tap Confirm → console.warn fires.
  </acceptance_criteria>
  <done>EscapeHatch component shell is importable from `@/components/placement/escape-hatch`. Plan 04 wave 3 embeds it on PassageScreen + QuestionScreen + result page (SC4 "after placement" delivered). Plan 05 wave 4 replaces the stub `handleConfirm` body with the real `abortPlacementAction({ attemptId, reason })` call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → live Supabase DB | DDL goes here via DIRECT_DATABASE_URL (postgres superuser). The migration must be reviewed before apply. |
| schema.ts (types) → live DB | Drift here is silent. Types compile against schema.ts; live DB is the actual runtime contract. |
| EscapeHatch stub → no DB writes (Wave 0) | The stub handleConfirm does NOT write anything; Plan 05 replaces with the real abort action. Until Plan 05 lands, tapping Confirm is a UX-only no-op + console.warn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-schema-drift | Tampering / Repudiation | drizzle migration vs live DB | mitigate | Task 1.4 is [BLOCKING] — `pnpm db:migrate` runs immediately followed by `pnpm db:verify-columns` against information_schema.columns. Non-zero exit blocks Wave 1. |
| T-3-bundle-leak | Information Disclosure | src/app/, src/components/ | mitigate | Task 1.5 implements `tests/invariants/placement-bundle-leak.test.ts` immediately (not as todo) so the grep gate is active from Wave 0. **Runtime complement (BLOCKER 4 mitigation):** Plan 05 Task 5.3 and Plan 06 Task 6.3 add Playwright `page.waitForResponse()` assertions against the RSC wire payload — the response body for `/placement/{attemptId}` must NOT contain `'isCorrect'`, `'is_correct'`, or any correct-choice ID. Grep catches code-level leaks; Playwright catches runtime serialization bugs. Both are required. |
| T-3-rls-cross-parent | Information Disclosure / Elevation of Privilege | new attempts columns | accept (deferred to Plan 06) | New columns inherit table-level RLS on `attempts` automatically (the EXISTS-subquery USING + WITH CHECK clauses cover all columns). Plan 06 adds the Playwright cross-parent spec body to confirm the live behavior. Wave 0 only stubs the spec file. |

</threat_model>

<truths>
- **D-05** (placeholder bank): Wave 0 prepares schema for `is_placeholder` columns; actual seeding lands in Plan 02.
- **D-07** (additive migration): Wave 0 lands all 4 new `attempts` columns + `is_placeholder` on `texts`+`questions` + `choice_order` on `attempt_answers` + the `escape_hatch_reason` enum. **D-07 reconciliation:** uses existing `assignedLevelId` UUID FK (does NOT add a redundant `assigned_level` integer column).
- **D-04** (same hatch component reused for Phase 4 via `mode` prop): the component shell with the `mode: 'placement' | 'reader'` API lands in Task 1.7 so Plan 04 can embed it on the result page in wave 3 (SC4 "after placement"). Action wiring deferred to Plan 05.
</truths>

<verification>
- `pnpm tsc --noEmit` exits 0.
- `pnpm lint` exits 0.
- `pnpm build` exits 0 (production build compiles with the EscapeHatch shell).
- `pnpm test:run tests/invariants/placement-bundle-leak.test.ts` exits 0.
- `pnpm test:run tests/invariants/rls-coverage.test.ts` exits 0 (unchanged — no new tables).
- `pnpm db:verify-columns` exits 0 (live DB has all 7 new columns + enum).
- `pnpm e2e --grep placement --list` lists ≥ 10 placement cases across 3 spec files.
- `pnpm e2e --grep "placement-flow" --list` lists 3 cases (PLAC-01 + PLAC-05 happy-path + parent-manage display).
</verification>

<success_criteria>
Wave 0 is complete when (1) the live Supabase DB has the 6 new columns + 1 new enum verifiable via the new `db:verify-columns` script, (2) shadcn AlertDialog is importable from `@/components/ui/alert-dialog`, (3) the EscapeHatch component shell is importable from `@/components/placement/escape-hatch` and renders the two floating buttons + modal with a stubbed action handler (Plan 05 fills the body), (4) all 8 RESEARCH/VALIDATION-prescribed test files exist on disk and are runnable, (5) `placement-bundle-leak.test.ts` is fully implemented and passing (not a stub — this is a standing invariant), (6) `pnpm tsc --noEmit && pnpm lint && pnpm build && pnpm test:run` are all green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-placement-vertical/03-01-SUMMARY.md` documenting (a) the exact SQL applied to the live DB, (b) the new `db:verify-columns` script and its output, (c) the 8 test file stubs created and which downstream plans own each one, (d) the EscapeHatch shell file and a note that its `handleConfirm` body is the PLAN-05-ACTION-WIRING insertion point.
</output>
