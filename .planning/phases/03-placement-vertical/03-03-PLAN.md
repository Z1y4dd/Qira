---
phase: 03-placement-vertical
plan: 03
slice_name: placement-algorithm-and-service
type: execute
wave: 2
depends_on:
  - 03-placement-vertical/01
  - 03-placement-vertical/02
files_modified:
  - src/services/placement.ts
  - src/services/placement.test.ts
  - src/services/placement.integration.test.ts
autonomous: true
requirements:
  - PLAC-01
  - PLAC-03
  - PLAC-08
threat_refs:
  - T-3-bundle-leak
  - T-3-deterministic-retake
mode: mvp

must_haves:
  truths:
    - "assignLevel() returns the correct level for every case in the RESEARCH coverage matrix, with bias-down applied BEFORE clamping (implements D-01 — fixed 15-item exam, bias-down by 1, grade-prior ± 3 clamp window with center per grade band)."
    - "deterministicShuffle(items, seed) produces the same order for the same seed across calls, and all input items survive (no loss, no duplicates)."
    - "getPlacementState(childId) returns 'not_started' | 'in_progress' | 'completed' | 'escape_hatched' based on a single indexed query against the latest placement attempt."
    - "startPlacement(childId) creates an attempts row with kind='placement', placementBankVersion=1, no assignedLevelId yet, returns the new attemptId (implements D-07 persistence shape — kind='placement' + additive columns; D-07 reconciliation: uses existing assignedLevelId FK, no redundant assigned_level integer)."
    - "getNextPlacementItem(attemptId) returns the next unanswered placement question for the attempt, with choices in deterministic shuffled order (using attemptId:questionId seed), and the choice objects DO NOT carry isCorrect."
    - "recordPlacementAnswer({attemptId, questionId, chosenChoiceId}) looks up choices.isCorrect server-side, writes attempt_answers row with is_correct + choice_order, returns {correct, nextItem, finalResult}. When all 15 questions answered, finalResult contains the assignedLevel and the service writes attempts.assignedLevelId + attempts.finishedAt."
    - "abortPlacement({attemptId, reason}) writes escape_hatched=true, escape_hatched_reason, escape_hatched_at, assigns fallback level via grade-prior center ± 1 (clamped), writes assignedLevelId + finishedAt."
    - "resetPlacement(childId) soft-archives the latest completed/escape-hatched attempt and returns; subsequent getPlacementState returns 'not_started'."
    - "Service Layer purity holds: src/services/placement.ts has zero next/* imports."
    - "Bundle-leak invariant holds: isCorrect appears only in src/services/* and src/db/*, never in src/app/* or src/components/*."
  artifacts:
    - path: src/services/placement.ts
      provides: "Full implementation of assignLevel (pure), deterministicShuffle (pure), getPlacementState, startPlacement, getNextPlacementItem, recordPlacementAnswer, abortPlacement, resetPlacement"
      exports: ["assignLevel", "deterministicShuffle", "getPlacementState", "startPlacement", "getNextPlacementItem", "recordPlacementAnswer", "abortPlacement", "resetPlacement", "PlacementItem", "PlacementResult", "PlacementState", "GRADE_PRIOR"]
    - path: src/services/placement.test.ts
      provides: "Full unit tests for the 3 pure functions (assignLevel, deterministicShuffle, abortPlacement formula)"
    - path: src/services/placement.integration.test.ts
      provides: "Full integration tests: getPlacementState/startPlacement/recordPlacementAnswer/abortPlacement/resetPlacement against the live test DB"
  key_links:
    - from: src/services/placement.ts
      to: src/db/schema.ts (attempts.escape_hatched, attempts.escape_hatched_reason, attempts.escape_hatched_at, attempts.placement_bank_version, attempt_answers.choice_order)
      via: drizzle insert/update
      pattern: "writes to new columns from Plan 01 migration"
    - from: src/services/placement.ts
      to: src/services/profiles.ts (ChildId, AuthError, GradeBand types)
      via: type import
      pattern: "import type { ChildId } from './profiles'; reuses AuthError class"
    - from: src/services/placement.ts
      to: levels table
      via: levels.number ↔ levels.id resolution
      pattern: "assignLevel returns integer level; service resolves to levels.id UUID for assignedLevelId FK write"
---

<objective>
Land the entire Service Layer for Phase 3 placement: the pure scoring function (`assignLevel`), the deterministic shuffle (`deterministicShuffle`), the state machine (`getPlacementState`), and the CRUD methods that drive the placement loop (`startPlacement`, `getNextPlacementItem`, `recordPlacementAnswer`, `abortPlacement`, `resetPlacement`). All functions remain framework-agnostic (NO `next/*` imports — enforced by `tests/invariants/service-layer-purity.test.ts`). Server-authoritative correctness evaluation lives inside `recordPlacementAnswer` (queries `choices.isCorrect`, writes `attempt_answers.is_correct`, returns `{correct: boolean}` — but NOT the answer key).

Purpose: PLAC-03 (deterministic rules-based algorithm with grade-prior + bias-down), PLAC-01 partial (the state machine that `(placement-gate)/layout.tsx` in Plan 06 will key off), PLAC-08 (writes attempts row with `kind='placement'` + `placementBankVersion=1`). This is the load-bearing slice — Plans 04, 05, 06 are all thin glue around this surface.

Output: src/services/placement.ts is complete and exports the canonical PlacementItem / PlacementResult / PlacementState types Plans 04+ consume; placement.test.ts and placement.integration.test.ts are fully implemented (no test.todo remaining) and all green.
</objective>

<execution_context>
@/home/ziyad/kids-AZ/.claude/get-shit-done/workflows/execute-plan.md
@/home/ziyad/kids-AZ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-placement-vertical/03-CONTEXT.md
@.planning/phases/03-placement-vertical/03-RESEARCH.md
@.planning/phases/03-placement-vertical/03-PATTERNS.md
@.planning/phases/03-placement-vertical/03-01-PLAN.md
@.planning/phases/03-placement-vertical/03-02-PLAN.md
@src/services/profiles.ts
@src/services/placement.ts
@src/db/schema.ts
@src/db/client.ts
@tests/invariants/service-layer-purity.test.ts

<interfaces>
<!-- The canonical Service Layer surface for Phase 3. Plans 04/05/06 consume these types directly. -->

```typescript
// From src/services/profiles.ts — reuse, do not redeclare:
export type ChildId = string & { readonly __brand: 'ChildId' };
export class AuthError extends Error { ... }
export type GradeBand = 'k' | '1-2' | '3-4' | '5-6'; // exists implicitly via ChildProfile['gradeBand']

// From src/services/placement.ts (already stubbed, fill in):
export type AttemptId = string & { readonly __brand: 'AttemptId' };
export type QuestionId = string & { readonly __brand: 'QuestionId' };
export type ChoiceId = string & { readonly __brand: 'ChoiceId' };

export interface PlacementItem {
  questionId: QuestionId;
  promptAr: string;          // NFC-normalized
  choices: { id: ChoiceId; labelAr: string }[];  // SHUFFLED, NO isCorrect
  level: number;             // The passage's level (2, 6, 10, 14, 18)
  passageTitleAr: string;
  passageBodyAr: string;
  questionIndex: number;     // 1..15
  totalQuestions: number;    // 15
}

export interface PlacementResult {
  attemptId: AttemptId;
  assignedLevel: number;     // 1–20
  escapeHatched: boolean;
  escapeHatchedReason: 'too_hard' | 'too_easy' | null;
}

export type PlacementState = 'not_started' | 'in_progress' | 'completed' | 'escape_hatched';

// New types this plan adds:
export interface PassageResult { level: number; totalQuestions: number; correctAnswers: number; }
export interface AssignLevelInput { passageResults: PassageResult[]; gradeBand: GradeBand; }
export interface AssignLevelOutput { assignedLevel: number; algorithmOutput: number; highestPassingLevel: number | null; }
export const GRADE_PRIOR: Record<GradeBand, { center: number; min: number; max: number }> = {
  'k':   { center: 2,  min: 1,  max: 5  },
  '1-2': { center: 5,  min: 2,  max: 8  },
  '3-4': { center: 9,  min: 6,  max: 12 },
  '5-6': { center: 14, min: 11, max: 17 },
};
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Fill placement.test.ts with assignLevel + deterministicShuffle + abort-formula tests</name>
  <files>src/services/placement.test.ts</files>
  <read_first>
    - src/services/placement.test.ts (Wave 0 stub from Plan 01)
    - src/services/placement.ts (existing stub — confirm imports + Zod schema for RecordPlacementAnswerInput)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §A "Vitest coverage matrix" (exhaustive case table)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §C "Choice order deterministic randomization" (shuffle spec)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §"Anti-pitfall 3" (bias-down must be tested explicitly)
  </read_first>
  <behavior>
    - assignLevel: "all correct, G1-2 → 8 (clamp.max — bias-down 18→17, then clamp to 8)" [explicit bias-down assertion per Anti-pitfall 3]
    - assignLevel: "all wrong, G1-2 → 4 (center 5 − 1, within clamp 2–8)"
    - assignLevel: "mixed: passes L2, L6, fails L10/L14/L18 in G1-2 → 5 (L6 − 1, clamped)"
    - assignLevel: "exactly 60% on L10, G3-4 → 9 (L10 − 1, clamped)"
    - assignLevel: "pre-K all wrong → 1 (center 2 − 1, clamped to min 1)"
    - assignLevel: "empty passageResults → uses grade-prior center" (edge — input.passageResults = [])
    - assignLevel: "G5-6 all correct → 17 (clamp.max)" (covers all 4 bands)
    - deterministicShuffle: "same seed → identical order across 10 calls" (idempotence)
    - deterministicShuffle: "5 distinct seeds → at least 3 distinct orders" (probabilistic; allows one rare collision)
    - deterministicShuffle: "all input items appear in output exactly once" (length and Set equality)
    - abortPlacement-formula: "too_hard at G3-4 (center 9) → 8 (clamped within 6–12)"
    - abortPlacement-formula: "too_easy at G3-4 → 10 (clamped within 6–12)"
    - abortPlacement-formula: "too_easy at pre-K (center 2, max 5) → 3" — but at the top: "too_easy at G5-6 (center 14, max 17) when raw would be 15 → 15 (still within clamp)"
    - abortPlacement-formula: "too_hard at pre-K → 1 (center 2 − 1 = 1, equal to min)"
  </behavior>
  <action>Replace all `test.todo` calls in `src/services/placement.test.ts` with real implementations. Import `assignLevel`, `deterministicShuffle`, and either `computeAbortFallback` (preferred, a named pure helper) or `GRADE_PRIOR` directly from `src/services/placement`. The abort-formula tests test a small pure function `computeAbortFallback(reason: 'too_hard' | 'too_easy', gradeBand: GradeBand): number` that Task 3.2 will export from `placement.ts`. All tests use Vitest's `describe`/`test`/`expect` — no mocking, no DB, no async. File runs in < 200ms.</action>
  <verify>
    <automated>pnpm test:run src/services/placement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 14 tests pass (or fail with clear messages pointing at the pure function being tested).
    - No `test.todo` calls remain.
    - Test file has zero DB imports (no `@/db/client`).
    - File runtime &lt; 500ms.
    - Each test name describes the assertion clearly (so failures are diagnosable).
    - **Anti-pitfall 3 mitigation:** at least one test explicitly comments "bias-down BEFORE clamping" so future readers cannot misread the algorithm.
  </acceptance_criteria>
  <done>Pure-function test surface is complete. Task 3.2 fills the implementation to make these green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.2: Implement placement.ts service layer (algorithm + state + CRUD)</name>
  <files>src/services/placement.ts</files>
  <read_first>
    - src/services/placement.ts (existing stubs — preserve the typed signatures + Zod schema for RecordPlacementAnswerInput)
    - src/services/profiles.ts (exact analog for service shape: imports, error class, Drizzle query pattern, branded types, NO next/* imports)
    - src/db/schema.ts (post-Plan 01 — confirm new column names + types)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §A (algorithm) + §C (server-authoritative scoring) + §D `getPlacementState` (one-row query)
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`src/services/placement.ts` (imports + branded types + lazy-import patterns)
    - tests/invariants/service-layer-purity.test.ts (the gate that fails if `from 'next/'` appears)
  </read_first>
  <behavior>
    - **Pure functions** (no DB, no async, no I/O):
      - `assignLevel(input)` per RESEARCH §A reference implementation with bias-down BEFORE clamping
      - `deterministicShuffle<T>(items: T[], seed: string): T[]` using `createHash('sha256')` from `node:crypto` per RESEARCH §C
      - `computeAbortFallback(reason: 'too_hard' | 'too_easy', gradeBand: GradeBand): number` — formula: `clamp(prior.center + (reason === 'too_hard' ? -1 : +1), prior.min, prior.max)`
      - `GRADE_PRIOR` constant exported for tests + Plan 04 use
    - **Stateful functions** (DB access via `db` from `@/db/client`):
      - `getPlacementState(childId)`: single query against `attempts` ordered by `startedAt desc limit 1`, returns `'not_started' | 'in_progress' | 'completed' | 'escape_hatched'`. Logic per RESEARCH §D.
      - `startPlacement(childId)`: insert `attempts` row with `kind: 'placement'`, `childId`, `placementBankVersion: 1`, default `startedAt = now()`, `finishedAt = null`, `escapeHatched = false`. Returns the new `AttemptId`.
      - `getNextPlacementItem(attemptId)`: (1) load the attempt + verify it's in_progress (else throw). (2) Find which placement questions have not yet been answered by joining `attempt_answers WHERE attemptId = ?` against the placement question set (all `questions WHERE is_placeholder = true ORDER BY level, position`). (3) For the next unanswered question, fetch the question + its text (passage) + its 4 choices. (4) Apply `deterministicShuffle(choices, \`${attemptId}:${questionId}\`)`. (5) Return a `PlacementItem` with `choices.map(c => ({ id: c.id, labelAr: c.textAr }))` — **strip `isCorrect`** (bundle-leak mitigation). Compute `questionIndex` (1-based) and `totalQuestions = 15`.
      - `recordPlacementAnswer({ childId, attemptId, questionId, chosenChoiceId })`: validate input via the existing `RecordPlacementAnswerInput` Zod schema. (1) Load the chosen choice from DB — server-side correctness lookup (`isCorrect: 0 | 1`). (2) Re-derive the shuffled order via `deterministicShuffle` with the same seed `attemptId:questionId`; store the resulting choice ID array as `choiceOrder: JSONB` on the new `attempt_answers` row. (3) Insert into `attempt_answers` with `attemptId`, `questionId`, `chosenChoiceId`, `isCorrect: chosen.isCorrect`, `choiceOrder`. (4) Check whether this was the 15th answer: count `attempt_answers WHERE attemptId = ?`. (5) If &lt; 15: return `{ correct: chosen.isCorrect === 1, nextItem: await getNextPlacementItem(attemptId), finalResult: null }`. (6) If = 15: aggregate per-passage accuracy (group `attempt_answers` by their question's `text.level`), call `assignLevel({ passageResults, gradeBand })` where gradeBand comes from `child_profiles.grade_band` (look up via `attempts.childId`). Look up `levels.id` for `levels.number = assignedLevel` (D-07 reconciliation — use existing `assignedLevelId` FK). Update the attempt: `assignedLevelId`, `finishedAt = now()`, `score = totalCorrect`. Return `{ correct, nextItem: null, finalResult: { attemptId, assignedLevel, escapeHatched: false, escapeHatchedReason: null } }`.
      - `abortPlacement({ attemptId, reason })`: validate `reason` is `'too_hard' | 'too_easy'`. Load attempt + child profile (for gradeBand). Compute `fallbackLevel = computeAbortFallback(reason, child.gradeBand)`. Look up `levels.id` for that integer. Update attempt: `escapeHatched: true`, `escapeHatchedAt: now()`, `escapeHatchedReason: reason`, `assignedLevelId: <fallback level id>`, `finishedAt: now()`, `score: 0` (or null — pick null for clarity; the attempt did not complete). Return `{ attemptId, assignedLevel: fallbackLevel, escapeHatched: true, escapeHatchedReason: reason }`.
      - `resetPlacement(childId)`: soft-archive the latest completed/escape-hatched placement attempt. Soft-archive = update its `kind` is preserved but the next `startPlacement` should be allowed. Simplest: ALL we need is `getPlacementState` to return `'not_started'` after reset. Two options: (a) hard-delete the latest attempt + its answers (cascade) — clean, no archival column. (b) Add an `archived_at` column. Pick (a): hard-delete is simpler, the user already had to confirm via the parent reset button. After delete, `getPlacementState` naturally returns `'not_started'`.
    - **Invariants**: zero `next/*` imports. `isCorrect` is read inside the service but NEVER returned in `PlacementItem.choices`. The `RecordPlacementAnswerInput` Zod schema (already present) is the only entry-point validation.
  </behavior>
  <action>Fill in the existing stubs in `src/services/placement.ts`. Preserve the existing typed signatures and the `RecordPlacementAnswerInput` Zod schema. Add new exports per the `<interfaces>` block above. Import patterns: `import { and, desc, eq, inArray, sql } from 'drizzle-orm'`; `import { db } from '@/db/client'`; `import { attemptAnswers, attempts, childProfiles, choices, levels, questions, texts } from '@/db/schema'`; `import type { ChildId } from './profiles'`; `import { AuthError } from './profiles'` (reuse, do not redeclare); `import { createHash } from 'node:crypto'`. The lazy-import pattern for `active-child-cookie` from profiles.ts line 89 is NOT needed here — placement.ts only takes injected types, never reads cookies. NO `cookies()`, NO `redirect()`, NO `revalidatePath()` — those belong in Plan 04's `actions.ts`. The `recordPlacementAnswer` `RecordPlacementAnswerInput` schema currently includes `childId` (line 43) — keep it, the caller passes it explicitly.</action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; pnpm test:run src/services/placement.test.ts &amp;&amp; pnpm test:run tests/invariants/service-layer-purity.test.ts &amp;&amp; pnpm test:run tests/invariants/placement-bundle-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test:run src/services/placement.test.ts` — all unit tests from Task 3.1 pass (algorithm + shuffle + abort-formula).
    - `pnpm test:run tests/invariants/service-layer-purity.test.ts` exits 0 (no `next/*` import in placement.ts).
    - `pnpm test:run tests/invariants/placement-bundle-leak.test.ts` exits 0 (isCorrect is not in placement.ts's exported types or in src/app or src/components — service-internal usage is fine).
    - `placement.ts` exports `assignLevel`, `deterministicShuffle`, `computeAbortFallback`, `GRADE_PRIOR`, `getPlacementState`, `startPlacement`, `getNextPlacementItem`, `recordPlacementAnswer`, `abortPlacement`, `resetPlacement`.
    - `PlacementItem.choices` type contains `{id, labelAr}` and **does not** contain `isCorrect` (T-3-bundle-leak mitigation at the type level).
    - `placement.ts` contains zero occurrences of `from 'next/` (grep test).
  </acceptance_criteria>
  <done>Service layer is complete and unit-tested. Plans 04/05/06 can import the full surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.3: Fill placement.integration.test.ts with full-loop assertions</name>
  <files>src/services/placement.integration.test.ts</files>
  <read_first>
    - src/services/placement.integration.test.ts (Wave 0 stub from Plan 01)
    - tests/e2e/_helpers/test-parents.ts (admin-client + createTestParent helper analog)
    - src/services/profiles.ts (the lazy parent-upsert pattern — integration tests bootstrap a parent + child via admin API)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §F "Integration test" (canonical scenario list)
  </read_first>
  <behavior>
    - Test 1: `getPlacementState(newChildId) === 'not_started'` for a child with zero attempts.
    - Test 2: After `startPlacement(childId)`, `getPlacementState(childId) === 'in_progress'`; the returned attemptId is a valid UUID; `attempts` row exists with `kind='placement'`, `placementBankVersion=1`, `assignedLevelId=null`, `finishedAt=null`, `escapeHatched=false`.
    - Test 3: `getNextPlacementItem(attemptId)` returns a `PlacementItem` with `questionIndex=1`, `totalQuestions=15`, `choices.length === 4`, and `choices[*]` has only `{id, labelAr}` keys (no `isCorrect`).
    - Test 4: Calling `recordPlacementAnswer` 14 times advances `questionIndex` and the `finalResult` is `null`; on the 15th call, `finalResult` is non-null and contains `assignedLevel` within 1..20. After the 15th call, `getPlacementState(childId) === 'completed'` and `attempts.assignedLevelId` is set.
    - Test 5: `abortPlacement({ attemptId, reason: 'too_hard' })` on an in-progress attempt: `attempts.escapeHatched = true`, `escape_hatched_reason = 'too_hard'`, `finishedAt` is set, `assignedLevelId` resolves to `gradePrior.center - 1` clamped. `getPlacementState(childId) === 'escape_hatched'`.
    - Test 6: `resetPlacement(childId)` after a completed attempt: `getPlacementState(childId) === 'not_started'` (the prior attempt is deleted, `attempt_answers` cascade).
    - Test 7: Choice order persistence: after `recordPlacementAnswer`, the `attempt_answers.choice_order` JSONB column equals the `deterministicShuffle(originalChoices, '${attemptId}:${questionId}')` output (re-derive in the test and compare).
    - Test 8: Cross-child isolation: `getPlacementState(childB)` returns `'not_started'` when childA has a completed attempt (sanity, not the full Playwright RLS test — that's Plan 06).
  </behavior>
  <action>Replace `test.todo` stubs in `src/services/placement.integration.test.ts` with full implementations. Use the admin-client bootstrap pattern from `tests/e2e/_helpers/test-parents.ts` (lines 19–29) to create test parents + children with unique emails, then clean up in `afterAll`. Use the `db` proxy from `@/db/client` for direct DB assertions on `attempts` and `attempt_answers` rows. Each test cleans up its created attempts in `afterEach` (or uses unique childIds per test to avoid cross-test contamination). The test DB is the same Supabase project — the `is_placeholder` bank seeded in Plan 02 must exist (test fails clearly if it doesn't, with the same "Run `pnpm db:seed:placement` first" message as Plan 02). Reuse the answer-selection helper: a small in-test fn `pickFirstChoice(item: PlacementItem)` that returns `item.choices[0].id`. Do not check correctness — these tests verify the *flow*, not the algorithm (the algorithm has its own pure tests in Task 3.1).</action>
  <verify>
    <automated>pnpm test:run src/services/placement.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests pass against the live test DB.
    - No `test.todo` calls remain.
    - Tests clean up created parents/children in `afterAll` (Supabase test DB stays uncluttered).
    - Tests use `createTestParent`/`deleteTestParent` from `tests/e2e/_helpers/test-parents.ts` (no duplicate admin-client setup).
    - File runtime ≤ 30s (per VALIDATION.md sampling rate).
  </acceptance_criteria>
  <done>Service layer integration coverage matches RESEARCH §F. Placement flow is provably correct end-to-end at the service level. Plan 04 can build the UI knowing the service is solid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action (Plan 04) → placement.ts | All inputs validated by Zod schemas inside service. Service does NOT trust caller-supplied `isCorrect`; it queries the DB. |
| placement.ts → DB | All writes use `db` proxy + branded types. No SQL string concatenation. |
| placement.ts → PlacementItem return value | `choices` is the bundle-crossing payload. `isCorrect` is stripped at the service boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-bundle-leak | Information Disclosure | PlacementItem.choices return type | mitigate | `PlacementItem.choices` type is declared `{ id: ChoiceId; labelAr: string }[]` — `isCorrect` is not in the type. The bundle-leak invariant (Plan 01) greps `src/app/` + `src/components/`; placement.ts lives in `src/services/` and is allowed to read `isCorrect` for server-authoritative scoring. |
| T-3-deterministic-retake | Tampering / Repudiation | deterministicShuffle | mitigate | `deterministicShuffle` uses a string seed (`attemptId:questionId`). Task 3.1 includes a unit test asserting same-seed → same-order across 10 calls. `attempt_answers.choice_order` JSONB persists the shuffled order at submit time so audit/retake is reproducible (Task 3.3 Test 7). |
| T-3-escape-hatch-bypass | Tampering | abortPlacement reason argument | mitigate | The `reason` argument is validated against the literal union `'too_hard' | 'too_easy'` inside `abortPlacement` (TS type + runtime check). Plan 05's escape-hatch component passes only these two strings; no free-text. |

</threat_model>

<truths>
- **D-01** (fixed 15-item exam, bias-down by 1, grade-prior clamp): implemented in `assignLevel()` with the exact GRADE_PRIOR table from CONTEXT. Unit tests in Task 3.1 cover all four grade bands.
- **D-02** (passage screen → one-question-per-page; choice order deterministic seed `attemptId+questionId`): `getNextPlacementItem` returns one item at a time; `deterministicShuffle(choices, '${attemptId}:${questionId}')` is the seeding rule.
- **D-03** (escape hatch = immediate abort + fallback level from grade-prior center ± 1, clamped): `abortPlacement` + `computeAbortFallback` implement this exactly.
- **D-07** (additive `attempts` columns + `attempt_answers.choice_order`): service writes to `escape_hatched`, `escape_hatched_reason`, `escape_hatched_at`, `placement_bank_version`, and `choice_order`. **D-07 reconciliation:** service uses the existing `assignedLevelId` FK and looks up `levels.id` by `levels.number`; does NOT touch an `assigned_level` integer column (none was added).
- **D-04** partial (same hatch component reused for Phase 4 via `mode` prop): the service-side action `abortPlacement` is what `mode="placement"` calls. The Phase 4 `mode="reader"` path will call a different service function in Phase 4 (`shiftLevelOneStep` or similar) — NOT implemented here.
</truths>

<verification>
- `pnpm tsc --noEmit` exits 0.
- `pnpm test:run src/services/placement.test.ts` — all unit tests pass (≥ 14 cases).
- `pnpm test:run src/services/placement.integration.test.ts` — all integration tests pass (8 cases).
- `pnpm test:run tests/invariants/service-layer-purity.test.ts` — `placement.ts` has no `next/*` import.
- `pnpm test:run tests/invariants/placement-bundle-leak.test.ts` — still passes (no Plan 04 yet; service-internal `isCorrect` doesn't count).
- Manually confirm: `PlacementItem.choices` type definition does not list `isCorrect`.
</verification>

<success_criteria>
The Service Layer is feature-complete for Phase 3. The algorithm produces deterministic biased-down clamped levels for every grade band, the state machine returns the correct PlacementState for every history shape, the placement loop persists rows correctly with all new columns, the escape-hatch fallback formula matches D-03, and `resetPlacement` returns the child to `'not_started'`. Plans 04/05/06 can compose against this surface without reading the service source.
</success_criteria>

<output>
After completion, create `.planning/phases/03-placement-vertical/03-03-SUMMARY.md` documenting (a) the final exported surface of `src/services/placement.ts` (function signatures only — no implementation code), (b) the integration test pass rate, (c) a note that the `mode="reader"` Phase 4 service hook is NOT implemented here and Phase 4 must add it.
</output>
