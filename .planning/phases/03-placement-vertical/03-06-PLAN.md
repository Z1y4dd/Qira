---
phase: 03-placement-vertical
plan: 06
slice_name: placement-gate-and-parent-reset
type: execute
wave: 5
depends_on:
  - 03-placement-vertical/01
  - 03-placement-vertical/03
  - 03-placement-vertical/04
  - 03-placement-vertical/05
files_modified:
  - src/app/(authenticated)/(active)/(placement-gate)/layout.tsx
  - src/app/(authenticated)/(active)/(placement-gate)/dashboard/page.tsx
  - src/app/(authenticated)/(active)/dashboard/page.tsx
  - src/app/(authenticated)/(picker)/profiles/[childId]/manage/page.tsx
  - src/app/(authenticated)/(picker)/profiles/[childId]/manage/actions.ts
  - src/app/(authenticated)/(picker)/profiles/[childId]/manage/reset-placement-form.tsx
  - tests/e2e/placement-cross-parent.spec.ts
  - tests/e2e/placement-flow.spec.ts
autonomous: true
requirements:
  - PLAC-01
  - PLAC-05
  - PLAC-07
threat_refs:
  - T-3-rls-cross-parent
mode: mvp

must_haves:
  truths:
    - "A new child profile (placement_state = 'not_started') landing on /dashboard is redirected to /placement/start by the (placement-gate) layout."
    - "A child with placement_state = 'in_progress' landing on /dashboard is redirected to /placement/{attemptId} (resumes the latest attempt)."
    - "A child with placement_state = 'completed' OR 'escape_hatched' freely accesses /dashboard (escape_hatched children received an assigned fallback level per D-03 and have a valid level — they pass the gate; D-06 literal text says 'redirect them' but D-03 takes precedence per reconciliation below)."
    - "On /profiles/[childId]/manage, the parent sees a 'حالة التقييم' card with one of four states: 'لم يبدأ بعد' / 'جاري التقييم' / 'المستوى المُعيَّن: ٤' / 'تم تخطي التقييم — المستوى التقريبي: ٣'."
    - "The parent can tap 'إعادة التقييم' (Reset placement) which calls resetPlacementAction; after confirmation, the child's placement_state returns to 'not_started' and the parent is redirected back to /profiles/[childId]/manage."
    - "Playwright tests/e2e/placement-cross-parent.spec.ts has at least 2 passing tests proving Parent A's placement rows are invisible to Parent B (RLS isolation, cookie-forge → /choose-child)."
    - "Playwright tests/e2e/placement-flow.spec.ts has 3 passing tests covering PLAC-01 gate redirect + PLAC-05 result screen + PLAC-05 parent-manage assignment display (filled from Plan 01 Task 1.6 stubs)."
  artifacts:
    - path: src/app/(authenticated)/(active)/(placement-gate)/layout.tsx
      provides: "Nested route group layout: calls getPlacementState, redirects unplaced children to /placement/start"
      contains: "getPlacementState"
    - path: src/app/(authenticated)/(picker)/profiles/[childId]/manage/page.tsx
      provides: "Modified — adds the 'حالة التقييم' card between 'بياناتك' (data export) and 'منطقة الخطر' (danger zone)"
    - path: src/app/(authenticated)/(picker)/profiles/[childId]/manage/actions.ts
      provides: "Adds resetPlacementAction({ childId, confirmName }) — server-side reset"
    - path: src/app/(authenticated)/(picker)/profiles/[childId]/manage/reset-placement-form.tsx
      provides: "Client component: button + AlertDialog confirmation that calls resetPlacementAction"
    - path: tests/e2e/placement-flow.spec.ts
      provides: "MODIFIED — fills in 3 test.fixme stubs from Plan 01 Task 1.6: (a) PLAC-01 gate redirect; (b) PLAC-05 result screen full-loop assertion; (c) PLAC-05 parent-manage assignment display."
  key_links:
    - from: src/app/(authenticated)/(active)/(placement-gate)/layout.tsx
      to: src/services/placement.ts (getPlacementState)
      via: server-side function call
      pattern: "if state === 'not_started' || state === 'in_progress' → redirect to /placement/start (escape_hatched and completed children pass; see Task 6.1 reconciliation)"
    - from: src/app/(authenticated)/(picker)/profiles/[childId]/manage/reset-placement-form.tsx
      to: src/app/(authenticated)/(picker)/profiles/[childId]/manage/actions.ts (resetPlacementAction)
      via: form action + AlertDialog confirmation
      pattern: "client component wraps Server Action in AlertDialog (analog: delete-dialog.tsx)"
    - from: src/app/(authenticated)/(active)/dashboard/page.tsx
      to: src/app/(authenticated)/(active)/(placement-gate)/dashboard/page.tsx
      via: file move (route restructure)
      pattern: "dashboard now lives inside (placement-gate) so the gate covers it; the old path is deleted"
---

<objective>
Close out Phase 3 with the two pieces that make the placement vertical a real product surface: (a) a placement-state gate that routes unplaced children into the assessment before they can reach the dashboard (and Phase 4's library), and (b) the parent-facing visibility + reset on `/profiles/[childId]/manage`. Also fill (b1) the cross-parent Playwright spec that proves RLS isolation on the new `attempts` rows AND (b2) the new `placement-flow.spec.ts` happy-path spec stubbed by Plan 01 Task 1.6 (PLAC-01 gate redirect + PLAC-05 result + PLAC-05 parent-manage display). After this plan, every PLAC requirement (01–08) has at least one passing automated assertion AND the VALIDATION.md grep target `placement-flow` has populated bodies.

Purpose: PLAC-01 (gate routes unplaced children to placement before the library is available — even though library is Phase 4, this gate is set up here so it's ready), PLAC-05 (parent sees the assignment in profile view), PLAC-07 (parent can reset and child can retake), T-3-rls-cross-parent (Playwright verification that parent A's placement is invisible to parent B), and the BLOCKER 1 mitigation (placement-flow.spec.ts bodies filled).

Output: A child with `not_started` state navigating to `/dashboard` is redirected to `/placement/start`. A parent visiting `/profiles/[childId]/manage` sees their child's placement status + a reset button. The Playwright cross-parent spec AND the placement-flow spec both pass.
</objective>

<execution_context>
@/home/ziyad/kids-AZ/.claude/get-shit-done/workflows/execute-plan.md
@/home/ziyad/kids-AZ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-placement-vertical/03-CONTEXT.md
@.planning/phases/03-placement-vertical/03-RESEARCH.md
@.planning/phases/03-placement-vertical/03-PATTERNS.md
@.planning/phases/03-placement-vertical/03-03-PLAN.md
@src/app/(authenticated)/(active)/layout.tsx
@src/app/(authenticated)/(active)/dashboard/page.tsx
@src/app/(authenticated)/(picker)/profiles/[childId]/manage/page.tsx
@src/app/(authenticated)/(picker)/profiles/[childId]/manage/actions.ts
@src/app/(authenticated)/(picker)/profiles/[childId]/manage/delete-dialog.tsx
@src/services/placement.ts
@src/services/profiles.ts
@tests/e2e/placement-cross-parent.spec.ts
@tests/e2e/placement-flow.spec.ts
@tests/e2e/auth-cross-user.spec.ts
@tests/e2e/_helpers/test-parents.ts

<interfaces>
The `(placement-gate)` route group adds NO URL segment (route group convention). The directory restructure:

Before this plan:
```
src/app/(authenticated)/(active)/
  layout.tsx              (requireActiveChild)
  dashboard/page.tsx
```

After this plan:
```
src/app/(authenticated)/(active)/
  layout.tsx              (unchanged — requireActiveChild)
  (placement-gate)/
    layout.tsx            (NEW — getPlacementState + redirect)
    dashboard/page.tsx    (moved here — gate now wraps it)
```

URLs do not change (route groups have no URL segment).

`resetPlacementAction` signature (added to `manage/actions.ts`):
- `'use server'`
- `export async function resetPlacementAction(_prev, formData: FormData): Promise<{ error?: string }>`
- Reads `childId` and `confirmName` from formData (analog: existing `deleteChildAction`).
- Validates parent owns the child via `requireParent + getChildProfile`.
- Validates `confirmName.normalize('NFC') === child.displayName.normalize('NFC')` (same gate as delete-dialog).
- Calls `await resetPlacement(childId as ChildId)` (Plan 03 service).
- Redirects to `/profiles/${childId}/manage`.

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Create (placement-gate) route group + move dashboard into it</name>
  <files>src/app/(authenticated)/(active)/(placement-gate)/layout.tsx, src/app/(authenticated)/(active)/(placement-gate)/dashboard/page.tsx, src/app/(authenticated)/(active)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(authenticated)/(active)/layout.tsx (the existing requireActiveChild layout — confirm its export pattern + how it returns the `active` child)
    - src/app/(authenticated)/(active)/dashboard/page.tsx (the existing dashboard page — moved verbatim into the gate group)
    - src/services/placement.ts (Plan 03 — getPlacementState signature)
    - .planning/phases/03-placement-vertical/03-RESEARCH.md §D "App Router Gating — Placement State Check" + "Recommended approach: dedicated (placement-gate) route group layout"
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`src/app/(authenticated)/(placement-gate)/layout.tsx`
    - .planning/phases/03-placement-vertical/03-CONTEXT.md §D-03 and §D-06 (for the reconciliation below)
  </read_first>
  <reconciliation>
    **D-06 vs D-03 reconciliation (revision 1, WARN 2):** CONTEXT D-06 literal text says: "if the active child's `placement_state` is `'not_started'` or `'escape_hatched'` and the user hits `/dashboard` ... redirect them." CONTEXT D-03 states: "Tap during placement = immediate abort + fallback level from age/grade prior ... Server records the partial attempt with `escape_hatched: true`, computes fallback level = grade-prior clamp center − 1 (or + 1) ... routes to the result screen. ... `assignedLevelId` is set." The two decisions conflict on what to do with an escape-hatched child arriving at /dashboard. **D-03 takes precedence:** escape-hatched children received a valid `assignedLevelId` (the grade-prior fallback) and have a level to read at — they are not "unplaced." Redirecting them to `/placement/start` would force them to re-take the assessment they explicitly opted out of, which contradicts the entire purpose of the escape hatch (relief valve, not a do-over). The parent retains control via `/profiles/[childId]/manage`'s reset button if they want to force a retake. Implementation: this gate redirects ONLY when `state === 'not_started'` or `state === 'in_progress'`; `'completed'` and `'escape_hatched'` both pass through. This reconciliation is recorded as a `<truth>` entry below and the in-file comment in the new layout.tsx will explicitly cite "per D-03 precedence over D-06" so a future reviewer cannot mis-read the algorithm.
  </reconciliation>
  <action>(1) Create `src/app/(authenticated)/(active)/(placement-gate)/layout.tsx` — Server Component layout. `export const dynamic = 'force-dynamic'` and `export const revalidate = 0`. Body: read cookies, create supabase, call `requireActiveChild(supabase, cookieStore.get(ACTIVE_CHILD_COOKIE)?.value)` to get `active` (this layout is nested inside `(active)/layout.tsx` but route groups don't share parent layout's awaited values — re-call). Catch `AuthError` with reason `NO_ACTIVE_CHILD` → `redirect('/choose-child')`. Then `const state = await getPlacementState(active.id);`. Per the `<reconciliation>` block above, redirect ONLY when state is unplaced: `if (state === 'not_started' || state === 'in_progress') redirect('/placement/start');` Otherwise (`'completed'` OR `'escape_hatched'`) → render `<>{children}</>`. Add an in-file comment IMMEDIATELY ABOVE the if-redirect line: `// D-03 takes precedence over D-06: escape_hatched children received a grade-prior fallback level and are not 'unplaced' — they pass the gate. Parent can force a retake via /profiles/[childId]/manage reset.` (2) Move `src/app/(authenticated)/(active)/dashboard/page.tsx` to `src/app/(authenticated)/(active)/(placement-gate)/dashboard/page.tsx` — file move only, no content change. (3) Delete the now-empty `src/app/(authenticated)/(active)/dashboard/` directory. (4) Verify URL behavior: `/dashboard` still resolves (route groups have no URL segment). The placement routes `/placement/*` are siblings of `(active)` under `(authenticated)/placement/` (Plan 04 created them outside `(active)` to avoid the gate looping — verify this is still the case).</action>
  <verify>
    <automated>test -f src/app/\(authenticated\)/\(active\)/\(placement-gate\)/layout.tsx &amp;&amp; test -f src/app/\(authenticated\)/\(active\)/\(placement-gate\)/dashboard/page.tsx &amp;&amp; ! test -f src/app/\(authenticated\)/\(active\)/dashboard/page.tsx &amp;&amp; grep -q "getPlacementState" src/app/\(authenticated\)/\(active\)/\(placement-gate\)/layout.tsx &amp;&amp; grep -q "/placement/start" src/app/\(authenticated\)/\(active\)/\(placement-gate\)/layout.tsx &amp;&amp; grep -q "force-dynamic" src/app/\(authenticated\)/\(active\)/\(placement-gate\)/layout.tsx &amp;&amp; grep -q "D-03 takes precedence over D-06" src/app/\(authenticated\)/\(active\)/\(placement-gate\)/layout.tsx &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm build &amp;&amp; bash scripts/lint-force-dynamic.sh</automated>
  </verify>
  <acceptance_criteria>
    - New layout file exists at `src/app/(authenticated)/(active)/(placement-gate)/layout.tsx` with `force-dynamic`.
    - Dashboard page lives at `src/app/(authenticated)/(active)/(placement-gate)/dashboard/page.tsx`.
    - The old `src/app/(authenticated)/(active)/dashboard/page.tsx` is gone.
    - Layout calls `getPlacementState` and conditionally redirects to `/placement/start` ONLY when state is `'not_started'` or `'in_progress'` (NOT for `'escape_hatched'` — per the D-03 reconciliation comment).
    - The in-file reconciliation comment `D-03 takes precedence over D-06` is present (grep returns ≥ 1).
    - `pnpm build` exits 0.
    - `bash scripts/lint-force-dynamic.sh` exits 0 (every layout including the new one declares force-dynamic).
    - Manual smoke (`pnpm dev`): new child with no placement → navigate to `/dashboard` → redirected to `/placement/start`. Same child after completing placement → navigate to `/dashboard` → renders the dashboard. Escape-hatched child → navigate to `/dashboard` → renders the dashboard (NOT redirected — the fallback level is valid).
  </acceptance_criteria>
  <done>Placement-state gate is live. Unplaced and in-progress children land in placement; placed and escape-hatched children land in dashboard. The D-03/D-06 reconciliation is documented in-file. Plan 04's placement routes stay outside the gate so no redirect loop.</done>
</task>

<task type="auto">
  <name>Task 6.2: Add 'حالة التقييم' card + reset action on /profiles/[childId]/manage</name>
  <files>src/app/(authenticated)/(picker)/profiles/[childId]/manage/page.tsx, src/app/(authenticated)/(picker)/profiles/[childId]/manage/actions.ts, src/app/(authenticated)/(picker)/profiles/[childId]/manage/reset-placement-form.tsx</files>
  <read_first>
    - src/app/(authenticated)/(picker)/profiles/[childId]/manage/page.tsx (the existing manage page — Card pattern, ordering between bayanak/danger-zone)
    - src/app/(authenticated)/(picker)/profiles/[childId]/manage/actions.ts (the existing deleteChildAction — exact analog for the new resetPlacementAction)
    - src/app/(authenticated)/(picker)/profiles/[childId]/manage/delete-dialog.tsx (closest analog for the reset-placement-form Client Component — confirmName gate + useActionState + AlertDialog)
    - src/services/placement.ts (getPlacementState + resetPlacement signatures)
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`src/app/(authenticated)/(picker)/profiles/[childId]/manage/page.tsx (modified)`
  </read_first>
  <action>(1) **Modify `manage/page.tsx`** to add a new card between the existing "بياناتك" (data export) Card and the "منطقة الخطر" (danger zone) Card. The new card has header `<ArabicText as="h2" size="ui" className="text-lg font-semibold block">حالة التقييم</ArabicText>` and a body that renders one of four states based on `await getPlacementState(profile.id)` — also resolve `assignedLevel` from the latest attempt if the state is `'completed'` or `'escape_hatched'` (single query: `select assignedLevelId, escape_hatched, escape_hatched_reason from attempts where childId = ? and kind = 'placement' order by startedAt desc limit 1`, then join `levels.number`). State copy: `'not_started'` → "لم يبدأ بعد"; `'in_progress'` → "جاري التقييم"; `'completed'` → "المستوى المُعيَّن: <bdi dir='ltr'>{level}</bdi>"; `'escape_hatched'` → "تم تخطي التقييم — المستوى التقريبي: <bdi dir='ltr'>{level}</bdi>". Below the state line, embed `<ResetPlacementForm childId={profile.id} childName={profile.displayName} state={state} />` (the Client component from step 3). When state is `'not_started'`, the reset button is rendered disabled (nothing to reset). (2) **Add `resetPlacementAction` to `manage/actions.ts`** mirroring `deleteChildAction` shape: `'use server'`; `export async function resetPlacementAction(_prev: ResetActionState | undefined, formData: FormData): Promise<ResetActionState>`. Read `childId` and `confirmName` from formData. If missing → return `{ error: 'معرّف الطفل أو الاسم مفقود' }`. Create supabase; call `requireParent`; then `getChildProfile(supabase, childId)` to verify ownership. If `child === null` → return `{ error: 'الجلسة منتهية أو الملف غير موجود' }`. Check `child.displayName.normalize('NFC') === confirmName.normalize('NFC')` else `{ error: 'الاسم غير مطابق' }`. Call `await resetPlacement(childId as ChildId)` (Plan 03 service). Then `redirect(\`/profiles/\${childId}/manage\`)`. Define `export interface ResetActionState { error?: string; }` near the existing `DeleteActionState`. (3) **Create `manage/reset-placement-form.tsx`** as `'use client'` mirroring `delete-dialog.tsx`: props `{ childId: string; childName: string; state: PlacementState }`. Uses `useState` + `useActionState`. Renders a `<Button variant="outline" size="lg" className="w-full" disabled={state === 'not_started'}>إعادة التقييم</Button>` that opens an `<AlertDialog>` asking "هل تريد فعلاً إعادة التقييم؟" with a text input requiring the parent to type the child's display name (same confirmName gate as delete-dialog) plus an "AlertDialogAction" `<Button type="submit" variant="default" disabled={pending}>{pending ? 'جارٍ…' : 'تأكيد'}</Button>` inside a `<form action={formAction}>`. Pass `childId` + the typed `confirmName` as hidden form inputs.</action>
  <verify>
    <automated>grep -q "حالة التقييم" src/app/\(authenticated\)/\(picker\)/profiles/\[childId\]/manage/page.tsx &amp;&amp; grep -q "resetPlacementAction" src/app/\(authenticated\)/\(picker\)/profiles/\[childId\]/manage/actions.ts &amp;&amp; test -f src/app/\(authenticated\)/\(picker\)/profiles/\[childId\]/manage/reset-placement-form.tsx &amp;&amp; grep -q "'use client'" src/app/\(authenticated\)/\(picker\)/profiles/\[childId\]/manage/reset-placement-form.tsx &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm build &amp;&amp; bash scripts/lint-rtl.sh &amp;&amp; pnpm test:run tests/invariants/placement-bundle-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `manage/page.tsx` contains the literal `"حالة التقييم"` (grep ≥ 1 match).
    - `manage/actions.ts` exports `resetPlacementAction` and `ResetActionState`.
    - `manage/reset-placement-form.tsx` exists with `'use client'`.
    - The reset button is disabled when state is `'not_started'`.
    - `pnpm build` exits 0.
    - `bash scripts/lint-rtl.sh` exits 0.
    - `pnpm test:run tests/invariants/placement-bundle-leak.test.ts` exits 0.
    - Manual smoke: parent on `/profiles/[childId]/manage` for a child with completed placement sees "المستوى المُعيَّن: ٤" (or similar); tap "إعادة التقييم" → type child's name → tap "تأكيد" → redirected back; the status card now reads "لم يبدأ بعد"; navigating to `/dashboard` redirects to `/placement/start`.
  </acceptance_criteria>
  <done>Parent has visibility into placement status and can reset. PLAC-05 + PLAC-07 are observable. Task 6.3's placement-flow.spec.ts has a real "المستوى المُعيَّن:" UI surface to assert against.</done>
</task>

<task type="auto">
  <name>Task 6.3: Fill Playwright placement-cross-parent.spec.ts AND placement-flow.spec.ts</name>
  <files>tests/e2e/placement-cross-parent.spec.ts, tests/e2e/placement-flow.spec.ts</files>
  <read_first>
    - tests/e2e/placement-cross-parent.spec.ts (Wave 0 stub from Plan 01 — has test.fixme to convert)
    - tests/e2e/placement-flow.spec.ts (Wave 0 stub from Plan 01 Task 1.6 — has 3 test.fixme to convert: PLAC-01 gate, PLAC-05 result, PLAC-05 parent-manage)
    - tests/e2e/auth-cross-user.spec.ts (exact analog — cross-parent isolation pattern, cookie forge)
    - tests/e2e/_helpers/test-parents.ts (createTestParent, deleteTestParent, signIn helpers)
    - src/app/(authenticated)/(picker)/profiles/[childId]/manage/page.tsx (from Task 6.2 — confirms the "المستوى المُعيَّن:" copy the spec asserts against)
    - .planning/phases/03-placement-vertical/03-PATTERNS.md §`tests/e2e/placement-cross-parent.spec.ts`
    - .planning/phases/03-placement-vertical/03-VALIDATION.md (PLAC-01 + PLAC-05 mapping to `pnpm e2e --grep 'placement-flow'`)
  </read_first>
  <action>**Part A — `tests/e2e/placement-cross-parent.spec.ts`:** Replace `test.fixme` calls with real implementations. Use `test.describe.serial` and `beforeAll`/`afterAll` with `createTestParent` + `deleteTestParent`. Test 1 — "Parent A completes placement; Parent B cannot read A's attempts row": (a) Create test parents A and B with unique emails; (b) For Parent A: open browser context A, sign in, create a child profile via the existing profile-create flow (or via admin API for speed), navigate to `/placement/start`, complete the 15 questions by tapping `choice cards` in order; (c) Capture the `attemptId` from the result page URL (`/placement/{attemptId}/result`); (d) For Parent B: open browser context B, sign in as B, attempt to visit `/placement/${attemptId}/result` (i.e., A's attemptId) — assert the page either 404s or redirects elsewhere (RLS would cause the result-page query to return null → redirect to /placement/{attemptId} → which getNextPlacementItem would fail on; depending on how Plan 04 handles "attempt not found", assert one of: redirected to `/choose-child`, or shown an error, or the URL is no longer `/placement/${attemptId}/result`. The KEY assertion: Parent B never sees the rendered assigned level for Parent A's attempt). Test 2 — "Parent B forges qira_active_child to A's child UUID → redirected to /choose-child": (a) Get Parent A's child UUID from the admin API; (b) In Parent B's context, set the `qira_active_child` cookie to that UUID via `context.addCookies(...)`; (c) Navigate to `/dashboard`; (d) Assert the URL becomes `/choose-child` (because `requireActiveChild` re-validates the parent-child link and throws `NO_ACTIVE_CHILD`). **Part B — `tests/e2e/placement-flow.spec.ts` (REVISION-1 BLOCKER-1 mitigation):** Replace the 3 `test.fixme` calls from Plan 01 Task 1.6 with real implementations. Use the same `createTestParent` + admin-API child-creation pattern. Test 1 — "Unplaced child navigates to /dashboard → redirected to /placement/start" (PLAC-01 gate): create parent + child, sign in, navigate to `/dashboard`, assert `await expect(page).toHaveURL(/\/placement\/start$/);`. Test 2 — "Full 15-question loop completes → result screen renders 'اخترنا لك المستوى' with a level number inside &lt;bdi dir='ltr'&gt;" (PLAC-05): start placement, advance past passage 1, tap the first choice card for each of the 15 questions (4 passage advancements + 15 answers), assert the URL ends with `/result`, assert `await expect(page.getByText('اخترنا لك المستوى')).toBeVisible();` and assert a `<bdi dir="ltr">` element with text matching `/^\d+$/` (a Western digit 1-20) is present. Test 3 — "Parent on /profiles/[childId]/manage sees 'المستوى المُعيَّن:' (or similar Western-digit level) after child completes placement" (PLAC-05 parent visibility): from the same context where Test 2 completed placement, navigate to `/profiles/[childId]/manage`, assert `await expect(page.getByText('المستوى المُعيَّن:')).toBeVisible();`. Note: the level value itself is inside a `<bdi>` adjacent to the label per Task 6.2's render — assert that as well: `await expect(page.locator('bdi[dir="ltr"]').filter({ hasText: /^\d+$/ })).toBeVisible();`. Use `test.describe.serial()` to keep the three tests in order; reuse the parent + child created in Test 1 across all three (cleanup in `afterAll`).</action>
  <verify>
    <automated>pnpm e2e tests/e2e/placement-cross-parent.spec.ts &amp;&amp; pnpm e2e tests/e2e/placement-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/placement-cross-parent.spec.ts` has at least 2 non-fixme tests; `pnpm e2e` on the file exits 0.
    - `tests/e2e/placement-flow.spec.ts` has at least 3 non-fixme tests (gate, result, parent-manage); `pnpm e2e` on the file exits 0.
    - Combined test runtime ≤ 180s (placement-flow is the long one — full 15-question loop).
    - **T-3-rls-cross-parent mitigation:** Playwright proves Parent B cannot read Parent A's placement state, even with a forged active-child cookie.
    - **BLOCKER-1 mitigation:** the VALIDATION.md grep target `pnpm e2e --grep 'placement-flow'` now has populated test bodies covering PLAC-01 and PLAC-05.
  </acceptance_criteria>
  <done>RLS cross-parent isolation is provably enforced for the new Phase 3 attempts columns. PLAC-01 gate redirect and PLAC-05 result + parent-manage display are E2E-verified end-to-end. PLAC-05 + PLAC-08 are E2E-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → (placement-gate) layout | Layout reads active child cookie, calls getPlacementState, redirects. No client-side decisioning. |
| Parent's manage page → resetPlacementAction | Action validates parent ownership via requireParent + getChildProfile + confirmName-typed gate. |
| Parent A's browser → Parent B's data | Existing RLS on attempts (Phase 1) + Service Layer `requireActiveChild` (Phase 2) prevent any cross-parent read/write. Playwright verifies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-rls-cross-parent | Information Disclosure / Elevation | new attempts columns | mitigate | Task 6.3 Part A adds 2 Playwright tests: (a) Parent B cannot SELECT Parent A's attempts row via any UI path, (b) cookie-forging A's child UUID into B's session triggers `NO_ACTIVE_CHILD` redirect. Both rely on the pre-existing Phase 1 RLS policies covering the new columns automatically (table-level RLS = column-level RLS). |
| T-3-gate-bypass | Elevation of Privilege | (placement-gate) layout | mitigate | The gate redirects unplaced/in-progress children to `/placement/start`. The placement routes themselves live OUTSIDE the gate group (`src/app/(authenticated)/placement/*` is sibling to `(active)/`), so the redirect doesn't loop. A child navigating directly to `/dashboard` cannot escape the gate — the layout always runs server-side. |
| T-3-reset-without-confirmation | Tampering | resetPlacementAction | mitigate | The action requires `confirmName` typed by the parent to match `child.displayName` (same gate as `deleteChildAction`). Accidental tap → modal → must type name correctly to confirm. |

</threat_model>

<truths>
- **D-06 + D-03 reconciliation** (placement-state gate behavior): D-06 literal text lists `'not_started'` and `'escape_hatched'` as redirect-to-placement states. D-03 establishes that escape-hatched children received a grade-prior fallback level and have a valid `assignedLevelId`. **D-03 takes precedence** because the escape hatch is the explicit relief valve — redirecting escape-hatched children back to placement would force them to re-take what they just opted out of. Implementation: gate redirects ONLY for `'not_started'` and `'in_progress'`; `'completed'` and `'escape_hatched'` both pass. The in-file comment in `(placement-gate)/layout.tsx` cites this reconciliation. Parents who want to force a retake use the reset button (Task 6.2).
- **D-04 part 2** (the parent reset is available from `/profiles/[childId]/manage`): implemented in Task 6.2 with the same Card + AlertDialog + confirmName pattern as the existing delete flow.
- **D-07** (RLS on new columns inherits from table-level): Playwright in Task 6.3 verifies cross-parent isolation against the new `escape_hatched`, `assignedLevelId`, etc. columns transitively.
- **PLAC-01** (placement gate routes unplaced children before library is available): Task 6.1 layout + Task 6.3 placement-flow.spec.ts Test 1 verify.
- **PLAC-05** (parent sees the assignment in profile view): the new "حالة التقييم" card shows the assigned level (or escape-hatched fallback) in Arabic with Western digit inside `<bdi dir="ltr">`. Task 6.3 placement-flow.spec.ts Test 3 verifies.
- **PLAC-07** (parent can reset and child can retake): `resetPlacementAction` → `resetPlacement` service → `getPlacementState` returns `'not_started'` → next navigation to `/dashboard` triggers gate → redirect to `/placement/start` → new attempt starts.
</truths>

<verification>
- `pnpm build` exits 0.
- `pnpm tsc --noEmit` exits 0.
- `bash scripts/lint-force-dynamic.sh` exits 0.
- `bash scripts/lint-rtl.sh` exits 0.
- `pnpm test:run tests/invariants/placement-bundle-leak.test.ts` exits 0.
- `pnpm e2e tests/e2e/placement-cross-parent.spec.ts` exits 0 (≥ 2 passing tests).
- `pnpm e2e tests/e2e/placement-flow.spec.ts` exits 0 (≥ 3 passing tests).
- `pnpm e2e tests/e2e/placement-escape-hatch.spec.ts` exits 0 (from Plan 05; ≥ 7 passing tests including the result-screen visibility + RSC wire-payload assertion).
- Manual smoke (`pnpm dev`): full happy-path E2E — new child → `/dashboard` → redirect to `/placement/start` → complete placement → land on result → navigate to `/dashboard` → renders dashboard (no redirect). Then on `/profiles/[childId]/manage`: see "المستوى المُعيَّن: 4" → reset → confirm → status reads "لم يبدأ بعد" → `/dashboard` → redirected to `/placement/start` again. Then test escape-hatched path: start placement, tap "هذا صعب", confirm → land on result with fallback level → navigate to `/dashboard` → renders dashboard (D-03 precedence — NOT redirected).
</verification>

<success_criteria>
Every PLAC requirement (01–08) is now observably implemented and has a passing automated assertion. The placement-state gate routes unplaced + in-progress children into placement before they can reach the dashboard (escape-hatched children pass per D-03 precedence). The parent has visibility + reset on the manage page. Cross-parent RLS isolation is proven by Playwright. The VALIDATION.md `placement-flow` grep target has populated bodies. Phase 3 is feature-complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-placement-vertical/03-06-SUMMARY.md` documenting (a) the final route tree (`(active)/(placement-gate)/dashboard` + sibling `placement/*`), (b) the manage page additions (status card + reset form), (c) the Playwright cross-parent + placement-flow test counts + scenarios, (d) the D-06/D-03 reconciliation as captured in the in-file comment, (e) a Phase-3 EXIT.md handoff note: "Literacy specialist review of placeholder bank is OUT OF SCOPE for Phase 3 — Phase 5 launch gate."
</output>
