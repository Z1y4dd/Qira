// Stub — replaced in Slice 3 with the real reset-request flow.
import { ArabicText } from '@/components/arabic-text';

export default function ResetRequestStub() {
  return (
    <ArabicText as="h1" size="reader" className="text-2xl block text-center">
      نسيت كلمة المرور؟
    </ArabicText>
  );
}
