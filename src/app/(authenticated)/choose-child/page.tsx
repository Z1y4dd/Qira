// Stub — replaced in Slice 5 with the real picker page.
import { ArabicText } from '@/components/arabic-text';

export default function ChooseChildStub() {
  return (
    <main className="min-h-screen flex items-center justify-center px-4">
      <ArabicText as="h1" size="reader" className="text-2xl">
        اختيار الطفل
      </ArabicText>
    </main>
  );
}
